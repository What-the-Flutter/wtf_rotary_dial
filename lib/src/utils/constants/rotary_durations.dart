abstract class RotaryDurations {
  static const Duration small = Duration(milliseconds: 200);
  static const Duration medium = Duration(milliseconds: 300);
  static const Duration large = Duration(milliseconds: 400);
  static const Duration extraLarge = Duration(milliseconds: 500);

  static const Duration smallSecond = Duration(seconds: 1);
  static const Duration largeSecond = Duration(seconds: 3);

  static const double intervalNone = 0;
  static const double intervalHalf = 0.5;
  static const double intervalInteger = 1;
}
